posts=/posts
posts.byId=/posts/{id}
posts.comments=/posts/{id}/comments

comments=/comments
comments.byId=/comments/{id}

albums=/albums
albums.byId=/albums/{id}

photos=/photos
photos.byId=/photos/{id}

todos=/todos
todos.byId=/todos/{id}

users=/users
users.byId=/users/{id}
